{
  "name": "ccg-grant-deliberation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "doctor": "node scripts/doctor.mjs",
    "check": "node --check scripts/runtime-environment.mjs && node --check scripts/doctor.mjs && node --check scripts/setup.mjs && node --check scripts/run-grant-deliberation.mjs",
    "setup": "node scripts/setup.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
