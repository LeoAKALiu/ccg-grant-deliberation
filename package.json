{
  "name": "ccg-grant-deliberation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "check": "node --check scripts/run-grant-deliberation.mjs && node --check scripts/install-home-plugin.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
